/**
 * @fileoverview Layout for accounts/settings pages
 *
 * Overrides the main layout to provide full-width layout for settings.
 */

export default function AccountsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
