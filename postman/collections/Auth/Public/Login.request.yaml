$kind: http-request
name: Login
description: Autentica l'utente con identifier e password. Salva accessToken e refreshToken come variabili di collection.
method: POST
url: "{{baseUrl}}/api/public/auth/login"
headers:
  Content-Type: application/json
body:
  type: json
  content: |
    {
      "identifier": "isabelladavis3@yahoo.com",
      "password": "password123"
    }
scripts:
  - type: afterResponse
    code: "var json = pm.response.json();\r

      pm.environment.set(\"accessToken\", json.accessToken);\r

      pm.environment.set(\"refreshToken\", json.refreshToken);\r

      pm.environment.set(\"expiresIn\", json.expiresIn);\r

      pm.environment.set(\"refreshExpiresIn\", json.refreshExpiresIn);"
    language: text/javascript
order: 1000
