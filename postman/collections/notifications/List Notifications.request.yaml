$kind: http-request
name: List Notifications
description: >
  Recupera la lista notifiche del destinatario autenticato.
  Error case: 401 se il bearer token manca o non è valido.
method: GET
url: "{{baseUrl}}/api/priv/notifications?limit=50"
headers:
  Authorization: Bearer {{accessToken}}
order: 1000
