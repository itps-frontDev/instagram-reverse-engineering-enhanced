<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="POST_LIKES;2026-05-07;cbiallo;01" author="cbiallo">
        <sql>
            CREATE TABLE post_likes (
                id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                profile_id BIGINT NOT NULL,
                post_id    BIGINT NOT NULL,
                created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
                deleted_at TIMESTAMPTZ,

                CONSTRAINT POST_LIKES_PROFILE_FK FOREIGN KEY (profile_id) REFERENCES profiles(id) ON DELETE CASCADE,
                CONSTRAINT POST_LIKES_POST_FK    FOREIGN KEY (post_id)    REFERENCES posts(id)    ON DELETE CASCADE
            );

            CREATE UNIQUE INDEX idx_post_likes_active  ON post_likes(profile_id, post_id) WHERE deleted_at IS NULL;
            CREATE INDEX        idx_post_likes_profile ON post_likes(profile_id)          WHERE deleted_at IS NULL;
            CREATE INDEX        idx_post_likes_post    ON post_likes(post_id)             WHERE deleted_at IS NULL;
        </sql>
        <rollback>
            DROP TABLE post_likes;
        </rollback>
    </changeSet>

</databaseChangeLog>
